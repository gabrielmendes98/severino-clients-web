import styled from '@emotion/styled';
import { GoogleLogin as RawGoogleLogin } from 'react-google-login';

export const GoogleLogin = styled(RawGoogleLogin)`
  width: 100%;
  justify-content: center;
`;
